apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-civil-manage-api.fullname" . }}
  labels:
    {{- include "laa-civil-manage-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laa-civil-manage-api.selectorLabels" . | nindent 4 }}
