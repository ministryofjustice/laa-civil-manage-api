apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laa-civil-manage.fullname" . }}-test-connection"
  labels:
    {{- include "laa-civil-manage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laa-civil-manage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
